{
  "rules": {
    "following": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    },
    "organizations": {
      ".read": true,
    	".write": true
    }
  }
}
