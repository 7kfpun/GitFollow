import { h } from 'preact';

const RateLimit = () => (
  <div className="flash flash-error">Rate limit reached. You had better login with you GitHub account to get the best experience with GitFollow.</div>
);

export default RateLimit;
